package Physical_layer.simulations;

simple SimulationControl
{
 parameters:
        //std::vector<Drone>& drone_data = default(nullptr);
    gates:
        input socketIn;
        output socketOut;   
}
